# Local development goes through the Vite dev proxy declared in vite.config.ts,
# because erebus-api registers no CORS middleware yet (ADR-003, LLD §15 item 8).
VITE_API_BASE_URL=/v1
VITE_REQUEST_TIMEOUT_MS=8000
